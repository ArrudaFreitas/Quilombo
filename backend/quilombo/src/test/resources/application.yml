spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
            scope: openid,email,profile

app:
  base-domain: quilombo.localhost
  jwt:
    secret: test-secret-key-for-junit-tests-only-not-production
    expiration-hours: 1
